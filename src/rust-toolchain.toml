# Exact-version pin, honored because the tool entry point always resolves cargo through rustup (tools/toolchains.py). Bumps are deliberate: update the version, rebuild everything, run the full test matrix. The wasm32-unknown-emscripten target gets added here in M2.
[toolchain]
channel = "1.96.1"
